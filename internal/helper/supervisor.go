package helper

func init() {
	// client, err := supervisord.NewClient(``)
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// // 获取Supervisor中所有进程的状态
	// processInfos, err := client.GetAllProcessInfo()
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// // 打印进程列表
	// for _, processInfo := range processInfos {
	// 	fmt.Printf("Process: %s, Group: %s, Status: %s\n",
	// 		processInfo.Name, processInfo.Group, processInfo.StateName)
	// }

}
